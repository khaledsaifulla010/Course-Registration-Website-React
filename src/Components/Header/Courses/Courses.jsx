import { useState } from "react";

const Courses = () => {
    const [courses, setCourses] = useState([]);
    
    return (
        <div>
            
        </div>
    );
};

export default Courses;